name: mental-health-env

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10  # ← Penting: Python 3.10, BUKAN 3.9
  - pip
  - pip:
      - mlflow==2.19.0
      - pandas
      - numpy
      - scikit-learn